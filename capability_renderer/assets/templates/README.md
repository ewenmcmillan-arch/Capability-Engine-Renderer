# Templates

Reserved for future report-type templates (nutrition card, blood-
pressure card, race-preview card, etc. — v2.0 roadmap: "Renderer
Framework, supports every Capability Engine report type"). Empty for
now; the mission-card layout is currently expressed directly as
geometry.PANEL_BOXES plus the panels/ modules rather than a template
file.
